{
  "name": "startpage-addon",
  "version": "1.0.0",
  "description": "Startpage Addon",
  "main": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:firefox": "web-ext build --source-dir firefox",
    "run:firefox": "web-ext run --source-dir firefox",
    "build:chrome": "cd chrome && web-ext build",
    "run:chrome": "cd chrome && web-ext run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "web-ext": "^7.11.0"
  }
}
